{
  "name": "VCPEIT — Operations brief (fictional example)",
  "nodes": [
    {
      "parameters": {},
      "id": "manual-trigger",
      "name": "Run example",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "return [\n  {\n    \"service\": \"Document sharing\",\n    \"status\": \"healthy\",\n    \"summary\": \"The fictional test account can access the intended sample folder.\",\n    \"owner\": \"Service owner A\",\n    \"evidenceRef\": \"fixture://sharing-check\"\n  },\n  {\n    \"service\": \"Internet failover\",\n    \"status\": \"watch\",\n    \"summary\": \"The secondary path is available; the application-level test remains to be scheduled.\",\n    \"owner\": \"Service owner B\",\n    \"evidenceRef\": \"fixture://failover-observation\"\n  },\n  {\n    \"service\": \"Backup recovery\",\n    \"status\": \"action\",\n    \"summary\": \"A restore job completed, but the restored application's usability has not been checked.\",\n    \"owner\": \"Service owner C\",\n    \"evidenceRef\": \"fixture://restore-job\"\n  }\n].map(json => ({ json }));"
      },
      "id": "sample-data",
      "name": "Fictional sample data",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        250,
        0
      ],
      "notesInFlow": true,
      "notes": "Fictional local input. Replace only after checking the input contract in the resource README."
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "// n8n Code node: Run Once for All Items. Produces a draft, never sends it.\nconst items = $input.all();\nconst allowed = new Set([\"healthy\", \"watch\", \"action\", \"unknown\"]);\nconst nonblank = value => typeof value === \"string\" && value.trim().length > 0;\nconst observations = items.map((item, index) => {\n  const input = item.json ?? {};\n  const issues = [];\n  if (!nonblank(input.service)) issues.push(\"Service name is missing.\");\n  if (!allowed.has(input.status)) issues.push(\"Status must be healthy, watch, action, or unknown.\");\n  if (!nonblank(input.summary)) issues.push(\"Observation is missing.\");\n  if (!nonblank(input.owner)) issues.push(\"Owner is unassigned.\");\n  if (!nonblank(input.evidenceRef)) issues.push(\"Evidence reference is missing.\");\n  return {\n    inputIndex: index,\n    service: nonblank(input.service) ? input.service : `Unnamed service (input ${index + 1})`,\n    status: issues.length ? \"unknown\" : input.status,\n    reportedStatus: allowed.has(input.status) ? input.status : null,\n    summary: nonblank(input.summary) ? input.summary : \"No observation supplied.\",\n    owner: nonblank(input.owner) ? input.owner : \"Unassigned\",\n    evidenceRef: nonblank(input.evidenceRef) ? input.evidenceRef : null,\n    issues\n  };\n});\nconst counts = { healthy: 0, watch: 0, action: 0, unknown: 0 };\nfor (const observation of observations) counts[observation.status] += 1;\nconst hasGaps = observations.length === 0 || counts.unknown > 0;\nconst attention = observations.filter(item => item.status !== \"healthy\");\nreturn [{\n  json: {\n    status: hasGaps ? \"needs_input\" : \"draft_ready\",\n    total: observations.length,\n    counts,\n    attention,\n    observations,\n    note: observations.length === 0\n      ? \"No observations supplied. This is not evidence of healthy services.\"\n      : \"A draft from supplied observations. References and service health have not been independently verified.\"\n  }\n}];\n"
      },
      "id": "evaluate",
      "name": "Operations brief",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        0
      ],
      "notesInFlow": true,
      "notes": "Inspect the returned status. This node has no external action and does not verify referenced evidence."
    }
  ],
  "connections": {
    "Run example": {
      "main": [
        [
          {
            "node": "Fictional sample data",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fictional sample data": {
      "main": [
        [
          {
            "node": "Operations brief",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": false,
  "pinData": {},
  "tags": []
}
